DATABASE_URL=sqlite:///./jobs.db

# Directory where your images actually live. image_path in POST /jobs is
# resolved relative to this, so images are never committed to the repo.
# Copy this file to .env and point it wherever you like, e.g.:
#   IMAGE_DIR=/home/you/Pictures/test-images
# IMAGE_DIR=./sample_images
IMAGE_DIR=/home/sanghamitrad/Documents/Assignments/python/images